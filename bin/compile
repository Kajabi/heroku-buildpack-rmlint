#!/usr/bin/env bash
# bin/compile <build-dir> <cache-dir> <env-dir>

set -eo pipefail

source "$(dirname $0)/support/environment.sh"
source "$(dirname $0)/support/ensure-rmlint.sh"

topic "Running .heroku/rmlint.sh"
cd $BUILD_DIR
"$BASH" .heroku/rmlint.sh
